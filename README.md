
  # PyscHire Design v2

  This is a code bundle for PyscHire Design v2. The original project is available at https://www.figma.com/design/p1VtrRqi0s23cKpCObMmli/PyscHire-Design-v2.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  